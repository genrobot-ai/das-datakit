// Generated by https://github.com/foxglove/schemas

syntax = "proto3";

import "google/protobuf/timestamp.proto";
import "header.proto";

package foxglove;

// A compressed image
message CompressedImage {
  // Timestamp of image
  google.protobuf.Timestamp timestamp = 1;

  // Frame of reference for the image. The origin of the frame is the optical center of the camera. +x points to the right in the image, +y points down, and +z points into the plane of the image.
  string frame_id = 4;

  // Compressed image data
  bytes data = 2;

  // Image format
  // 
  // Supported values: `webp`, `jpeg`, `png`
  string format = 3;

  // Encrypted metadata for internal identification.
  // Plaintext format: `GenrobotYYYYMMDDHHMMSS`.
  // Encryption: repeating-key XOR, then lowercase hex encoding.
  // Key: `GenrobotCameraMetaKey20260416`
  string metadata = 9;

  arnold.common.proto.Header header = 8;
}
